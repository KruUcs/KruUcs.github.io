import { ScoreBackgroundColorDirective } from './score-background-color.directive';

describe('ScoreBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScoreBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
